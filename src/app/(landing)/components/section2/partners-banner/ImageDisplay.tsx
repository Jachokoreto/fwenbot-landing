/**
 * This code was generated by Builder.io.
 */
import Image from "next/image";
import * as React from "react";

interface ImageDisplayProps {
  images: { src: string; alt: string }[];
}

const ImageDisplay: React.FC<ImageDisplayProps> = ({ images }) => {
  return (
    <div className="flex flex-row flex-wrap gap-10 justify-center max-w-full w-full h-full">
      {images.map((image, index) => (
        <Image
          key={index}
          loading="lazy"
          src={image.src}
          alt={image.alt}
          fill
          className="object-contain self-stretch my-auto aspect-[3.69] min-w-[240px] w-[255px]"
        />
      ))}
    </div>
  );
};

export default ImageDisplay;
